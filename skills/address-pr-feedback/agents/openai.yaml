interface:
  display_name: "Address PR Feedback"
  short_description: "Fetch, judge, and respond to PR review feedback"
  default_prompt: "Use $address-pr-feedback to process every reviewer comment on a pull request."
